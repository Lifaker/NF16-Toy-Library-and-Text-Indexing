#include "Ex1.h"
#include "Ex2.h"

int main()
{
    /* 1.1 et 1.2 Factorielle */
    //printf("%d\n%d", fact1(1), fact2(1));

    /*1.3 Déterminant d'une matrice */
    /*
    matrice m[DIM], m2[DIM];
    int taille_m;
    taille_m = saisie_matrice(m);
    affiche_matrice(m,taille_m);
    sous_matrice(m, m2, taille_m, 3);
    affiche_matrice(m2, taille_m-1);
    printf("det(M) = %f\n", det(m, taille_m));
    */

    /*1.4 Suite de Fibonacci */
    /*
    printf("%d",fibo1(10));
    */

    /*2. */
    printf("Hello world !");
    return 0;
}
